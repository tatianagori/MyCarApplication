<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mycarapplication">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyCarApplication">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity android:name=".cars.AddCarActivity" />
        <activity android:name=".carModels.audi.A3Activity" />
        <activity android:name=".carModels.audi.AOneActivity" />
        <activity android:name=".carModels.seat.LeonActivity" />
        <activity android:name=".carModels.seat.IbizaActivity" />
        <activity android:name=".carModels.toyota.YarisActivity" />
        <activity android:name=".carModels.toyota.AygoActivity" />
        <activity android:name=".carModels.bmw.ThreeSeriesActivity" />
        <activity android:name=".carModels.bmw.TwoSeriesActivity" />
        <activity android:name=".carModels.mercedes.BClassActivity" />
        <activity android:name=".carModels.mercedes.AClassActivity" />
        <activity android:name=".carModels.vw.GolfActivity" />
        <activity android:name=".carModels.vw.PoloActivity" />
        <activity android:name=".carModels.toyota.ToyotaModelsActivity" />
        <activity android:name=".carModels.seat.SeatModelsActivity" />
        <activity android:name=".carModels.bmw.BMWModelsActivity" />
        <activity android:name=".carModels.vw.VWModelsActivity" />
        <activity android:name=".carModels.mercedes.MercedesModelsActivity" />
        <activity android:name=".carModels.audi.AudiModelsActivity" />
        <activity android:name=".InfoActivity" />
        <activity android:name=".SettingsActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>